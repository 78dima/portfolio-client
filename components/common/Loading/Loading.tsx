import React from 'react';

import styles from './Loading.module.scss';

const Loading = () => (
  <div className={styles.ldsDualRingWrapper}>
    <div className={styles.ldsDualRing} />
  </div>
);

export default Loading;
